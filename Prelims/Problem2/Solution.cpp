#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef vector<int> vi;

int main() {
    // Take input
    int n;
    cin >> n;

    // Print the squares of the first n natural numbers
    for (int i = 1; i <= n; i++) {
        cout << i * i << ' ';
    }
}
